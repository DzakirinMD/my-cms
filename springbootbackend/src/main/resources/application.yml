spring:
  datasource:
    url: 'jdbc:postgresql://localhost:49153/my_cms'
    username: postgres
    password: postgrespw
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  error:
    include-message: always
  port: 8083